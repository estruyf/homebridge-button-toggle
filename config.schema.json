{
  "pluginAlias": "button-toggle",
  "pluginType": "accessory",
  "singular": false,
  "headerDisplay": "Button toggle plugin",
  "footerDisplay": "Plugin which enables you to keep a state for your button.",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Toggler",
        "required": true
      },
      "dependsOn": {
        "title": "Activation dependency",
        "description": "Names of the buttons it depends on to automatically turn on when all of them are active.",
        "type": "array",
        "items": {
          "title": "Button Name",
          "type": "string"
        },
        "default": []
      },
      "dependsOff": {
        "title": "Deactivation dependency",
        "description": "Names of the buttons it depends on to automatically turn off when all of them are inactive.",
        "type": "array",
        "items": {
          "title": "Button Name",
          "type": "string"
        },
        "default": []
      },
      "debug": {
        "title": "Debug",
        "type": "boolean",
        "default": false,
        "required": false
      }
    }
  },
  "form": null,
  "display": null
}